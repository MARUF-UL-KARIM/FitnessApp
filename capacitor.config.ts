import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mohammadmaruf.fitnesssimulator',
  appName: 'Fitness Simulator',
  webDir: 'dist'
};

export default config;
